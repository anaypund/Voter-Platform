## Packages
react-hook-form | Form handling
@hookform/resolvers | Zod resolvers for forms
framer-motion | Animations and page transitions
lucide-react | Icons (already in base, but listing for completeness)
clsx | Class name utility
tailwind-merge | Class name utility

## Notes
- Theme color is dynamic based on API config (defaults to saffron #ff9933)
- Voter search has two modes: EPIC and Name
- PDF generation returns a binary stream/blob, needs handling in frontend
- Admin panel needs to toggle public access
